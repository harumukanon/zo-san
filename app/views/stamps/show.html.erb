<p>
  <%= render 'add_owner'%><br />
  
  蔵書印id:<%= @stamp.id %><br />
  蔵書印文:<%= @stamp.face_string %><br />
  蔵書印画像:　
  <% if @stamp.stamp_image? %>
　　<%= image_tag @stamp.stamp_image.url %>
　<% else %>
　　<%= image_tag("no-image.png", :size => "200x200") %>
　<% end %>
　<br />
  画像出典:<%= @stamp.image_source %><br />
  注記:<%= @stamp.note %><br />
  
  <% if editor_signed_in? %>
      <%= link_to "edit", edit_stamp_path(@stamp) %>
    <% end %>
  
  <% if editor_signed_in? %>
      <%= link_to "delete", stamp_path(@stamp), method: :delete,
                                       data: { confirm: "蔵書印情報を削除する前に，印主情報/資料情報とのリンクを解除済であることを確認してください。蔵書印情報を削除すると元に戻せません。本当に削除しますか？" } %>
    <% end %>
  
  <%= render 'add_item' %>


