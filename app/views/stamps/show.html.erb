<p>
  <%= render 'add_owner'%><br />
  
  <%= t('stamp_id') %>:<%= @stamp.id %><br />
  <%= t('face_string') %>:<%= @stamp.face_string %><br />
  <%= t('stamp_image') %>:　
  <% if @stamp.stamp_image? %>
　　<%= image_tag @stamp.stamp_image.url %>
　<% else %>
　　<%= image_tag(asset_path("no-image.png"), :size => "200x200") %>
　<% end %>
　<br />
  <%= t('image_source') %>:<%= @stamp.image_source %><br />
  <%= t('note') %>:<%= @stamp.note %><br />
  
  <% if editor_signed_in? %>
      <%= link_to "edit", edit_stamp_path(@stamp) %>
    <% end %>
  
  <% if editor_signed_in? %>
      <%= link_to "delete", stamp_path(@stamp), method: :delete,
                                       data: { confirm: t('danger') } %>
    <% end %>
  
  <%= render 'add_item' %>


