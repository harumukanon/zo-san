<%= form_for(@stamp.relationships.build, remote: true) do |form| %>
  <div><%= hidden_field_tag :item_id, item.id %></div>
  <%= hidden_field_tag :stamp_id, @stamp.id %>
  <%= form.submit "押印ありとしてリンク", class: "btn btn-primary" %>
<% end %>